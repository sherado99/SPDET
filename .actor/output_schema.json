{
  "actorOutputSchemaVersion": 1,
  "title": "SETI Result",
  "properties": {
    "originalEmail": {
      "type": "string",
      "description": "The original cold email provided by the user.",
      "title": "Original Email",
      "template": "{{originalEmail}}"
    },
    "improvedEmail": {
      "type": "string",
      "description": "The email rewritten by Stech AI with a warmer, more honest tone.",
      "title": "Improved Email",
      "template": "{{improvedEmail}}"
    },
    "toneUsed": {
      "type": "string",
      "description": "The tone applied (e.g., warm, empathetic, friendly).",
      "title": "Tone Used",
      "template": "{{toneUsed}}"
    },
    "status": {
      "type": "string",
      "description": "The success or failure status of the processing (e.g., 'success' or 'error').",
      "title": "Status",
      "template": "{{status}}"
    },
    "timestamp": {
      "type": "string",
      "description": "The processing timestamp in ISO 8601 format.",
      "title": "Timestamp",
      "template": "{{timestamp}}"
    }
  },
  "required": ["originalEmail", "improvedEmail", "status"]
}
