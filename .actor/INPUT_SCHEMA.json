{
  "title": "Stech Email Tone Improver",
  "description": "Rewrite multiple emails to be warm, honest, and personal. Upload CSV (with optional column mapping) or provide JSON array.",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "csvFile": {
      "title": "CSV File (optional)",
      "type": "string",
      "description": "Upload a CSV file. If you use dynamic mapping, provide columnMapping and rejectionTemplate.",
      "editor": "fileupload"
    },
    "emails": {
      "title": "Emails Array (optional)",
      "type": "array",
      "description": "Alternative to CSV: provide an array of email objects.",
      "editor": "json",
      "prefill": [
        {
          "originalEmail": "Dear Sir, your application has been rejected.",
          "targetTone": "empathetic",
          "additionalInstructions": "Make it short"
        }
      ],
      "items": {
        "type": "object",
        "properties": {
          "originalEmail": { "type": "string", "title": "Original Email" },
          "targetTone": { "type": "string", "title": "Target Tone", "enum": ["warm and honest", "friendly", "empathetic", "encouraging", "professional-warm"] },
          "additionalInstructions": { "type": "string", "title": "Additional Instructions", "editor": "textarea" }
        },
        "required": ["originalEmail"]
      }
    },
    "columnMapping": {
      "title": "Column Mapping (for CSV)",
      "type": "object",
      "description": "Map placeholders to CSV column names. Example: { \"name\": \"Full Name\", \"position\": \"Job Title\" }",
      "editor": "json",
      "default": {}
    },
    "rejectionTemplate": {
      "title": "Rejection Template (for CSV)",
      "type": "string",
      "description": "Template with placeholders like {name}, {position}. Required if columnMapping is provided.",
      "editor": "textarea",
      "default": ""
    },
    "defaultTone": {
      "title": "Default Tone",
      "type": "string",
      "description": "Fallback tone if not specified per email.",
      "default": "warm and honest",
      "enum": ["warm and honest", "friendly", "empathetic", "encouraging", "professional-warm"]
    },
    "maxConcurrency": {
      "title": "Max Concurrency",
      "type": "integer",
      "description": "Number of emails to process simultaneously (1-20).",
      "minimum": 1,
      "maximum": 20,
      "default": 5
    },
    "timeout": {
      "title": "Timeout (seconds)",
      "type": "integer",
      "description": "Max wait time per email request.",
      "minimum": 10,
      "maximum": 300,
      "default": 60
    }
  }
}
