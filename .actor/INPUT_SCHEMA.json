{
  "title": "Stech Email Tone Improver (Batch + Dynamic Mapping)",
  "description": "Upload any CSV from your ATS, map columns, and SETI will generate warm rejection emails.",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "csvFile": {
      "title": "CSV File",
      "type": "string",
      "description": "Upload a CSV file from your ATS (any column names).",
      "editor": "fileupload"
    },
    "columnMapping": {
      "title": "Column Mapping",
      "type": "object",
      "description": "Map placeholders in your template to actual column names in the CSV.",
      "prefill": {
        "name": "Full Name",
        "position": "Job Title",
        "company": "Company Name"
      },
      "editor": "json"
    },
    "rejectionTemplate": {
      "title": "Rejection Email Template",
      "type": "string",
      "description": "Use {name}, {position}, {company} as placeholders. Example: 'Dear {name}, thank you for applying for {position}. Unfortunately...'",
      "editor": "textarea",
      "prefill": "Dear {name}, thank you for your application for the {position} position. After careful review, we have decided not to move forward. We wish you success in your job search."
    },
    "defaultTone": {
      "title": "Default Tone",
      "type": "string",
      "default": "warm and honest",
      "enum": ["warm and honest", "friendly", "empathetic", "encouraging", "professional-warm"]
    },
    "maxConcurrency": {
      "title": "Max Concurrency",
      "type": "integer",
      "minimum": 1,
      "maximum": 20,
      "default": 5,
      "description": "Number of emails to process simultaneously."
    },
    "timeout": {
      "title": "Timeout (seconds)",
      "type": "integer",
      "minimum": 10,
      "maximum": 300,
      "default": 60
    }
  },
  "required": ["csvFile", "columnMapping", "rejectionTemplate"]
}
